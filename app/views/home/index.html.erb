<span><p class="lead"><strong>Select User to see its metrics</strong></p>
<form action="<%= get_user_path %>" method = "POST" class="form-horizontal">
    <select name="user_id" id="user_id">
        <% @ids.each do |id| %>
        <option value="<%= id %>"><%= id %></option>
        <% end -%>
    </select>
    <button class="btn">Submit</button>
</form></span><hr>